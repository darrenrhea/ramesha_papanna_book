{
    // This was generated by irp_init_restructuredtext_publishing
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "publish",
            "type": "shell",  // this should say shell
            "command": "si",
            "args": [],
            "options": {
                "cwd": "${workspaceFolder}",
                "shell": {
                    "executable": "/bin/bash",
                    "args": ["--login"]
                }
            },
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "presentation": {
                "reveal": "silent"  // Doesn't open the integrated terminal unless there is an error
            }
        }
    ]
}
